# Shellify Agent Instructions

## Central development governance

This repository is explicitly enrolled in `evs625/Development-Governance`.

For development process, start with current `evs625/Development-Governance/main/OPERATING_PROTOCOL.md`. Then load only the central role module and on-demand mechanism module(s) selected by that core.

Do **not** copy, pin, summarize, or restate central `protocol/*` process prose locally. Central development process remains authoritative at its current `main` state.

Repository-specific product, architecture, security, privacy, build, test, release, and source-integrity authority remains local. In particular, preserve and follow the repository's existing `CLAUDE.md`, directory `README.md` instructions, and other project documentation when applicable.
